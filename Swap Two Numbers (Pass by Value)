#include <stdio.h>


void swap(int x, int y) {
    int temp;
    temp = x;
    x = y;
    y = temp;

    printf("After Swap: %d %d\n", x, y);
}

int main() {
    int a, b;

    
    scanf("%d %d", &a, &b);

   
    printf("Before Swap: %d %d\n", a, b);

    
    swap(a, b);

    return 0;
}
