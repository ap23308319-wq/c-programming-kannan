#include <stdio.h>

int main() {
    int n, i;
    long long sum = 0;

    scanf("%d", &n);

    int arr[n];
    int *ptr = arr;

    for(i = 0; i < n; i++) {
        scanf("%d", ptr + i);  
    }

    for(i = 0; i < n; i++) {
        sum += *(ptr + i);     
    }

    printf("%lld", sum);

    return 0;
}
